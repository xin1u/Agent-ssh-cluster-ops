interface:
  display_name: "GridLatch (Legacy Codex Name)"
  short_description: "Compatibility adapter for GridLatch"
  default_prompt: "Use GridLatch via $codex-ssh-cluster-ops and follow its canonical skill to perform only the requested policy-bounded operation."
