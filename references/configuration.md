# Legacy Reference Adapter

The maintained configuration guide is [`../skills/ssh-cluster-ops/references/configuration.md`](../skills/ssh-cluster-ops/references/configuration.md). Existing `codex-ssh-cluster-ops` installations should follow that guide; legacy policy paths and environment variables remain supported.
